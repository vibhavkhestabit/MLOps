apiVersion: v1
kind: Pod

metadata:
  name: nginx-simple
  labels:
    app: nginx
    environment: learning

spec:
  containers:
    - name: nginx
      image: nginx:1.25-alpine

      ports:
        - containerPort: 80