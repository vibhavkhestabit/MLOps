Continuing the Week 3 Day 6 capstone (AI log analyser). Big update since the last carry-forward — most of the infra/CI/CD work described there is now DONE, not pending. Here's the real current state:
Folder: Azure-Bootcamp/week3/day6 contains main.py, analyser.py, requirements.txt (pinned openai==1.58.1), .env.example, README.md, Dockerfile, aks_cluster.sh, azure_pipeline.yml, and manifests/deployment.yaml + manifests/service.yaml.
What's actually done and verified, end to end:

App itself: fully tested locally (503 no-config, 200 /readyz with config, 502 with fake-but-present credentials proving real network call attempt) — never touched again since, still correct.
AKS + ACR provisioned — resource group aks-bootcamp-week3-rg, AKS aks-bootcamp-week3, ACR acrbootcampweek3. Confirmed healthy via full kubectl discovery output.
Azure DevOps fully set up — new org/project, repo pushed, two service connections created (acr-connection-week3 Docker Registry, aks-connection-week3 Kubernetes — had to use "cluster admin credentials" option to dodge an RBAC role-binding 401 error).
Pipeline is GREEN end to end. Hit and fixed two real bugs along the way: (1) Dockerfile COPY app/... assumed a subfolder that doesn't exist — fixed to copy flat files directly; (2) deploy stage failed with "path does not exist" because Azure DevOps deployment jobs don't auto-checkout the repo — fixed by adding checkout: self as the first deploy step. Both fixes are saved in the files above.
App is LIVE on AKS right now, deployed via the pipeline, 2/2 pods Running, Service is type LoadBalancer with a real public IP. Confirmed via curl http://<external-ip>/healthz → {"status":"ok"}, from outside the cluster, for real.
/analyse/text and /analyse/upload correctly return 503 "not configured yet" on the live deployment too — same as local — since no real OpenAI credentials are wired in yet. This is expected, not a bug.

What's still genuinely pending — this is the actual remaining work:

Real Azure OpenAI credentials (Day 5 deliverable) — plug into .env locally first, confirm a real successful /analyse/text call before touching Azure at all.
Create an Azure Key Vault, store the OpenAI endpoint/key/deployment name as secrets.
Wire the Secrets Store CSI driver into deployment.yaml so those secrets land as the same AZURE_OPENAI_* env var names the app already expects — app code needs zero changes.
Flip the readiness probe in deployment.yaml from /healthz to /readyz (currently both probes point at /healthz, which always returns 200 regardless of OpenAI config — meaning Kubernetes will route real traffic to pods before they can actually serve /analyse/* correctly; this was a deliberate temporary choice to get the pipeline working, flagged in a code comment, not forgotten).
Push the updated manifest, let the pipeline redeploy.
Verify a real end-to-end log analysis call against the live external IP.
(Stretch, optional) Application Insights tracing on the OpenAI calls.

One open decision already made: no Ingress — Service stays type: LoadBalancer directly exposing a public IP, by explicit choice (simpler, skip Ingress).
Cost note: cluster has been left running with real billing (small but nonzero) since this session — AKS node + public IP + ACR Basic tier are the main costs regardless of app activity.