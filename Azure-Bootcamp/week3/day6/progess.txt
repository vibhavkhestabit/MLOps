Continuing the Week 3 Day 6 capstone (AI log analyser). Built on Day 4, fully locally verified before Day 5 — here's where things stand:
Folder: Azure-Bootcamp/week3/day6 contains main.py (FastAPI routes: /healthz, /readyz, /analyse/text, /analyse/upload, auto /docs), analyser.py (Azure OpenAI client logic, isolated from main.py on purpose), requirements.txt (pinned, including openai==1.58.1 — NOT 1.51.0, which has a known httpx conflict bug we already hit and fixed), .env.example, README.md.
Verified working on my machine: /healthz always 200. /readyz returns 503 with no config, 200 once AZURE_OPENAI_ENDPOINT/_API_KEY/_DEPLOYMENT are set. /analyse/text and /analyse/upload return 503 "not configured" with no config, and — tested with fake credentials — 502 "Connection error" once config is present but the endpoint is fake, proving the app correctly reaches the real Azure OpenAI call path. No mocked responses anywhere.
Day 5 is now complete — I have a real Azure OpenAI resource with a deployed GPT-4o model. Let's: (1) plug real credentials into .env and confirm a real successful /analyse/text call with actual log text, (2) write the Dockerfile, (3) re-run aks_cluster.sh with new Week 3 resource group/AKS/ACR names since Week 2's were deleted, (4) write deployment.yaml/service.yaml/ingress.yaml for this app (note: my Week 2 manifests reference flask-app, this app needs its own name), (5) adapt my Week 2 Day 4 azure-pipelines.yml — needs new build path, renamed deploy target, and an apply -f manifests/ step before set image since AKS is fresh and the Deployment object won't exist yet, (6) deploy and verify via the public Ingress URL.